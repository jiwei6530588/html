第一章
1样式表的三种方法1内部样式表2外部样式表3内联样式表。
2语法a单一元素的多个属性用；分开，若只有一个可以省略。
     b多个元素之间有逗号隔开、
     c css代码的注射 /*
     d 设计者的样式，离内容越近。就显示那个样式，强制优先级 !comportant
     e 层叠，继承，冲突,字体，颜色可以继承，而布局不可以继承
     f 如果值为若干个单词。则要加引号，除了这种情况不可以加引号
3选择器
     a常用选择器
	样式作用的对象叫做选择器,通用选择器*,元素选择器, id选择器加一个#，类选择器前面加一个
.给一个元素加上多个类名，制定某一个特定类P.class1
     b属性选择器 E[ATTR] 选择具有attr属性的E元素E[ATTR][ATTR2]
      进一步缩小范围
      E[ATTR="VAL"] 选择具有attr属性。且属性等于VAL的E元素
      E[attr~='val']选择具有attr属性,且属性值有多个等于VAL的E元素
      E[attr|=attr] 选择具有attr属性,且属性值是以VAL开头，并用链子符分割的字符串的E元素（不常用）
      E【ATTR ^='VAL'】以VAL开头的attributex元素 $以VAL结尾的attribute元素 *属性值包含VAL的元素
     c 关系选择器 
     后代选择器又称包含选择器，后代选择器可以选择作为某元素后代的元素 孙元素也在
     子元素选择器 UL>LI ,与后代选择器相比，子元素选择器只能选择作为某元素子元素的元素。缩小了选择的范围。 
     相邻选择器，E+F 选择贴在E元素之后的F元素
     兄弟选择器E~F 选择E元素所有兄弟元素。只有选择之后的元素



